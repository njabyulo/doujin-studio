{
  "name": "@doujin/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas/index.ts",
    "./types": "./src/types/index.ts"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "pnpm lint:code && pnpm lint:type",
    "lint:code": "eslint .",
    "lint:type": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo",
    "clean:all": "pnpm clean && rm -rf node_modules"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@doujin/config": "workspace:*",
    "@types/node": "^20.19.30",
    "eslint": "^9.39.2",
    "fast-check": "^4.5.3",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^3.0.0"
  }
}